# Getting Started

Install the package, run a check-only command, then bootstrap inert examples.
Do not start with provider dispatch. Installed examples and source-checkout
examples use different paths; follow the package README for the exact command.

Read `accepted`, `operationally_usable`, `settled`, blockers, and residuals
as separate fields. Acceptance never implies settlement.

